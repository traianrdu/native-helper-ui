import React from 'react'

/**
 * Creates the page
 * @returns default homepage
 */
export default function Home() {
  return (
    <div>
        LOGO
        {renderLogo()}
        {renderLogo()}
        {renderLogo()}
        {renderLogo()}
        {renderLogo()}
    </div>
    
  )
}

/**
 * Renders logo
 * @returns logo
 */
function renderLogo() {
    return (
        <div>
            log
        </div>
    )
}